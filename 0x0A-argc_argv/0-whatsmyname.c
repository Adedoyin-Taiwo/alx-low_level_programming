#include <stdio.h>

/**
* main - Prints the name of the program, then a new line.
* @argc: Arguments.
* @argv: Pointers to the arguments.
* Return: 0
*/

int main(int __attribute__((__unused__)) argc, char *argv[])
{
	printf("%s\n", argv[0]);
	return (0);
}
